__BASE__: [
  '../coco.yaml',
  './hyp.scratch.x.yaml',
  './yolov11-base.yaml'
]

ms_amp_level: O3
per_batch_size: 43
overflow_still_update: False
network:
  depth_multiple: 1.00  # scales module repeats
  width_multiple: 1.50  # scales convolution channels
  max_channels: 512

optimizer:
  momentum: 0.95